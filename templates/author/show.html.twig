{% extends 'base.html.twig' %}

{% block title %}{{ author.lastName }}{% endblock %}

{% block body %}
    <h1>{{ author.firstName }} {{ author.lastName }}</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr> #}
            {# <tr>
                <th>FirstName</th>
                <td>{{ author.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ author.lastName }}</td>
            </tr> #}
            <tr>
                <th>Né en</th>
                <td>{{ author.birthYear }}</td>
            </tr>
            <tr>
                <th>Mort en </th>
                <td>{{ author.deathYear }}</td>
            </tr>
            <tr>
                <th>Résumé</th>
                <td>{{ author.summary }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ author.content | raw }}</td>
            </tr>
        </tbody>
    </table>

	<h3>Ses oeuvres ...</h3>
	{% for book in author.books %}
		<h5><a href="{{ path('book_show', {'slug': book.slug}) }}">{{ book.title }}</a></h5>
	{% endfor %}

    <a href="{{ path('author_index') }}">back to list</a>

    <a href="{{ path('author_edit', {'slug': author.slug}) }}">edit</a>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
