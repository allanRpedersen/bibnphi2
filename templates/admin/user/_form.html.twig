{{ form_start(form) }}
    {{form_row(form.email)}}
    {{form_row(form.userRoles)}}

    {# {{ form_widget(form) }} #}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
