		<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
			<a class="navbar-brand" style="color:darkorange" href="{{ path('front') }}">Bib'n'Phi II</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarColor02">
				<ul class="navbar-nav mr-auto" style="align-items:center;">
					<li class="nav-item active">
						<a class="nav-link btn btn-outline-primary mx-1" href="{{ path('front') }}">Accueil <span class="sr-only">(current)</span></a>
					</li>
					<li class="nav-item">
						<a class="nav-link btn btn-outline-primary mx-1" href="{{ path('author_index') }}">Les auteur.e.s</a>
					</li>
					<li class="nav-item">
						<a class="nav-link btn btn-outline-primary mx-1" href="{{ path('book_index') }}">Les oeuvres</a>
					</li>

					{# {% if book is defined %}
					<h4 class="mx-2 my-0 py-2" style="color:rgb(190, 218, 165); text-align:center;">{{ book.title }}</h4>	
					{% else %}
					{% endif %}
 #}
					{% if is_granted('ROLE_USER') %}
					<a class="nav-link btn btn-outline-info mx-1" href="{{ path('book_new') }}" title="Ajouter une oeuvre"><i class="fa fa-plus" aria-hidden="true"></i></a>

					{% else %}
					<li class="nav-item">
						<a class="nav-link btn btn-outline-primary mx-1" href="#">A propos</a>
					</li>
					{% endif %}
				</ul>

				{# <form class="form-inline my-2 my-lg-0">
					<input class="form-control mr-sm-2" type="text" placeholder="Search">
					<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
				</form> #}

				<ul class="navbar-nav ml-auto">
				{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('admin_book_index',{sortBy: 'Title'}) }}">
						<i class="fas fa-wrench"></i>
						Admin
					</a>
				</li>
				{% endif %}
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
				<li class="nav-item dropdown active" {# style="float:right;"  #}>
					<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
						{{ app.user.email }}
						{# <img src="{{ vich_uploader_asset(app.user, 'imageFile')}}" alt="avatar" style="width: 30px; height:auto;"> #}
					</a>
					<div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
						{# <a class="dropdown-item" href="{{ path('user_show', { slug: app.user.slug, id: app.user.id }) }}"><i class="fas fa-user"></i> Mes informations</a>
						<a class="dropdown-item" href="{{ path('admin_account_dashboard') }}"><i class="fas fa-chess" aria-hidden="true"></i> Admin dashboard</a> #}
						<a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Déconnexion</a>
						<a class="dropdown-item" href="{{ path('user_updatepwd') }}"><i class="fas fa-user-lock"></i> Modif mot de passe</a>
					</div>
				</li>

				{% else %}
				<li class="nav-item">
					<a href="{{ path('app_login') }}" title="connexion.." class="nav-link btn" role="button" aria-haspopup="true" aria-expanded="false">
						<i class="fas fa-sign-in-alt"></i></a>
				</li>
				{% endif %}

				</ul>

			</div>
		</nav>

		<div class="container mt-1" style="padding-top: 55px">{{ include('partials/_flash_messages.html.twig') }}</div>
