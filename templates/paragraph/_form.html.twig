{{ form_start(form) }}
	<div class="row">
		<div class="col-md-12">
			{{ form_row(form.content, {'label': 'contenu du paragraphe :'}) }}
		</div>
	</div>
	<button class="button-new-update btn btn-info ">
		{{ button_label|default('Ajout') }}
	</button>
{{ form_end(form) }}
