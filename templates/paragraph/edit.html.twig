{% extends 'base.html.twig' %}

{% block title %}Edit Paragraph{% endblock %}

{% block body %}
<div class="container mt-1">
    <h3>Modification du paragraphe n° : {{paragraph.id}} </h3>

    {{ include('paragraph/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    <a href="#" onClick="history.go(-1);">Retour</a>

    {# <a href="{{ path('book_show_with_jump', {slug: paragraph.book.slug, whereToJump: paragraph.id}) }}">Retour</a> #}

    {{ include('paragraph/_delete_form.html.twig') }}
</div>
{% endblock %}
